{
  "name": "locus-browser",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev:desktop": "pnpm --filter @locus/browser-desktop dev",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@types/node": "25.0.3",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
